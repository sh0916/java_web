<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
	PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

<!-- tag(또는 node)의 순서에 주의 -->

	<!-- 전달인자로 null 을 줄수있는 셋팅 -->

	<!-- mapper 에서 사용할 타입이 길어서 짧게 별칭 지정 -->
	<typeAliases>
		<typeAlias type="com.spring.emp.dto.EmpDTO" alias="empDTO"/>
	</typeAliases>
	
</configuration>